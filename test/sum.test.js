import {sum} from "../src/sum.js";

test("test sum", () => {
    const res = sum(1, 2);

    expect(res).toBe(3)
})
test("test sum 2", () => {
    const res = sum(1, 2);

    expect(res).toBe(3)
})
test("test sum 3", () => {
    const res = sum(1, 2);

    expect(res).toBe(3)
})
